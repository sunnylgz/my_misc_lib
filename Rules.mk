#=============================================================================
#           compiler
#=============================================================================
CC=gcc
LD=gcc
AR=ar

#=============================================================================
#           build options
#=============================================================================
D=n
ifeq ($(D), y)
CFLAGS= -O0 -g
else
CFLAGS= -O3
endif
CFLAGS += $(EXT_CFLAGS)
ARFLAGS=
OPT_INCLUDE=
USR_LIBS=
LNFLAGS=# -lpthread -ldl -lm -lrt

#=============================================================================
#           path
#=============================================================================
PATH_LOGS=./log
PATH_LIBS=./lib
PATH_APP=./
LIBS_LOG=$(PATH_LOGS)/lib.log
APP_LOG=$(PATH_LOGS)/app.log
